//Denifine a velocidade, direção e angulo do tiro
speed = 10;
if (direction == 0) {
    direction = point_direction(x, y, mouse_x, mouse_y);
}

speed = 10;
image_angle = direction;

dono = "player";

velocidade_original = speed;